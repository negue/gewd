import { MorphdomDirective } from './morphdom.directive';

describe('MorphdomDirective', () => {
  it('should create an compRef', () => {
    const directive = new MorphdomDirective();
    expect(directive).toBeTruthy();
  });
});
